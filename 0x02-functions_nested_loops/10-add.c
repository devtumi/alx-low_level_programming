#include "main.h"
/**
 * add - Adds two integers and returns
 * the result
 *
 * @a: Integer one
 * @b: Integer two
 *
 * Return: Add of number one and number two.
 */
int add(int a, int b)
{
	return (a + b);
}
